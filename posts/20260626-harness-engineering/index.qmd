---
title: "Harness Engineering"
description: "OpenAI's Ryan Lopopolo reports on building a million-line product with zero hand-written code, where the engineering work moved from writing code to building the environment agents run in."
author: synesis
date: "2026-06-26"
image: cover.jpg
---

![Layered domain architecture with explicit cross-cutting boundaries, the rigid layering OpenAI enforces mechanically so agents can move fast without drift. Image from [OpenAI](https://openai.com/index/harness-engineering/).](cover.jpg){fig-align="center" data-lightbox-src="cover-full.jpg"}

[Ryan Lopopolo](https://github.com/lopopolo), writing for [OpenAI](https://openai.com/), reports on an experiment in [Harness Engineering: Leveraging Codex in an Agent-First World](https://openai.com/index/harness-engineering/): over five months a small team shipped an internal product of roughly a million lines of code with zero manually-written lines, every line produced by [Codex](https://openai.com/index/introducing-codex/) while humans steered.

What the article reports:

- **Starting from an empty repository**: the first commit landed in late August 2025, and Codex generated the initial scaffold, including the `AGENTS.md` that tells agents how to work there. Over five months it grew to roughly a million lines across about 1,500 merged pull requests, with the team going from three engineers to seven at around 3.5 PRs per engineer per day. The core rule held throughout: no manually-written code.

- **Redefining the engineer's role**: early progress was slow not because Codex was incapable but because the environment was underspecified, so the job became enabling the agent. Work went depth-first, and when something failed the question was "what capability is missing, and how do we make it both legible and enforceable for the agent?" Humans interact almost entirely through prompts, and review was pushed agent-to-agent, with Codex reviewing its own changes and requesting further agent reviews in a loop until the reviewers are satisfied.

- **Making the application legible**: as throughput rose, human QA capacity became the bottleneck, so the team exposed the running app to Codex. The app is bootable per git worktree, the Chrome DevTools Protocol is wired into the agent runtime, and logs and metrics are queryable via LogQL and PromQL. Prompts like "ensure service startup completes in under 800ms" become checkable, and single runs work a task for six hours or more, often overnight.

- **Repository knowledge as the system of record**: one big `AGENTS.md` failed, since it crowded out the task, rotted into stale rules, and resisted mechanical checks. It became a roughly 100-line table of contents pointing into a structured `docs/` tree of design docs, plans, and references. Plans are first-class versioned artifacts, the layout supports progressive disclosure, and linters plus a recurring "doc-gardening" agent keep it current.

- **Agent legibility as the goal**: the repository is optimized first for what Codex can read, on the principle that "*anything it can't access in-context while running effectively doesn't exist*," so knowledge in Slack, Google Docs, or people's heads has to be pushed into the repo. They favored "boring," composable technologies the agent can model, and sometimes reimplemented a subset of a library, such as their own concurrency helper instead of a generic package, rather than wrap opaque upstream behavior.

- **Enforcing invariants, not implementations**: a rigid layered architecture (Types → Config → Repo → Service → Runtime → UI, with cross-cutting concerns entering through Providers) is enforced mechanically by custom, agent-written linters and structural tests, alongside "taste invariants" like structured logging and file-size limits. The principle is to *enforce boundaries centrally and allow autonomy locally*, and output that doesn't match human style is fine as long as it stays correct, maintainable, and legible to the next agent run.

- **Throughput changes the merge philosophy**: with agent output far exceeding human attention, *the repository runs with minimal blocking merge gates*, short-lived pull requests, and test flakes handled by follow-up runs rather than blocking. Corrections are cheap and waiting is expensive, a tradeoff they call irresponsible at low throughput and often correct here.

- **What "agent-generated" covers**: everything, including product code and tests, CI and release tooling, internal developer tools, documentation, evaluation harnesses, review comments, repository-management scripts, and dashboard definitions. Humans stay in the loop at a higher layer, prioritizing work, translating user feedback into acceptance criteria, and validating outcomes.

- **Crossing into end-to-end autonomy**: from a single prompt, Codex can now validate the codebase, reproduce a reported bug, record a video of the failure, implement and validate a fix, record a second video, open a pull request, respond to feedback, remediate build failures, escalate only when judgment is required, and merge. *The report cautions this depends on the repository's specific structure and tooling and should not be assumed to generalize without similar investment.*

- **Entropy and garbage collection**: full autonomy means Codex replicates existing patterns, including the bad ones, so the codebase drifts. The team used to spend a fifth of every week hand-cleaning "AI slop," then replaced that with encoded "golden principles" and recurring cleanup agents that scan for deviations and open small refactors, treating technical debt like a high-interest loan paid down continuously.

- **What they are still unsure of**: the approach has held through internal launch and adoption, but they don't yet know how architectural coherence evolves over years in a fully agent-generated system, where human judgment matters most, or how it changes as models improve. Their summary is that *the discipline now shows up in the environment and feedback loops more than in the code*.

![Codex drives the app through the Chrome DevTools Protocol to reproduce a bug and validate its own fix in a loop. Image from [OpenAI](https://openai.com/index/harness-engineering/).](devtools-loop.jpg){fig-align="center" data-lightbox-src="devtools-loop-full.jpg"}

![What Codex can't see doesn't exist: knowledge in Google Docs, Slack, or people's heads is invisible to the agent until it is encoded into the repository. Image from [OpenAI](https://openai.com/index/harness-engineering/).](agent-knowledge.jpg){fig-align="center" data-lightbox-src="agent-knowledge-full.jpg"}

## My Notes

- **Two views of the same problem**: this report and a source-level read of Anthropic's Claude Code, [Dive into Claude Code: The Design Space of Today's and Future AI Agent Systems](/posts/20260623-claude-code-design-space/), emphasize different halves of the same system. OpenAI's account is mostly about the environment the agent works in (the repository as system of record, enforced layering, agent-to-agent review), and the Claude Code read is mostly about the agent program itself (a single agent loop, the permission system, the tools it exposes). Where they meet is the bet that the real engineering lives in the structure around the model rather than the model itself, which is also why the Claude Code analysis finds the loop nearly trivial and almost all the code in the systems wrapped around it.

- **The scarce resource is human attention**: the entire design treats human time and attention as the fixed constraint and reorganizes everything else around it. Legible logs, progressive-disclosure docs, a map instead of a manual: these are all ways to spend less human attention per unit of agent work. It's the same context-engineering discipline the field has been converging on, here applied at repository scale, and it generalizes past coding to any agentic pipeline. The move from a retrieval flow to a navigable repository the agent investigates on its own is the same reframing as [Filesystems vs. RAG](/posts/20260404-filesystem-vs-rag/).

- **The engineers moved up a layer**: the headline (a million lines, zero hand-written, a tenth of the time) is the part everyone repeats, but the report is careful that the end-to-end autonomy "depends heavily on the specific structure and tooling of this repository and should not be assumed to generalize without similar investment." Humans went from writing code to specifying intent, encoding invariants, and validating outcomes, which is the same division of labor argued in ["Skills" Are Not Software Engineering](/posts/20251124-skills-not-software-engineering/): the model proposes, and a deterministic layer of linters and structural tests enforces correctness.

- **Constraints enable the speed**: the team front-loaded the kind of rigid architecture you normally postpone until you have hundreds of engineers, because for agents the constraints are what allow speed without decay. The operational detail worth stealing is that the custom lint error messages are written to inject remediation instructions into the agent's context, so *the guardrail doubles as a teaching signal*. In a human workflow these rules would feel pedantic. With agents, once encoded they apply everywhere at once.

- **Autonomous generation produces entropy**: Codex replicates whatever patterns already exist, including the bad ones, so the codebase drifts. The team spent a fifth of every week hand-cleaning "AI slop" until that obviously didn't scale, then replaced it with encoded "golden principles" and recurring cleanup agents. A million lines in five months is also a million lines no human has read line by line, and the bet is that agent-legibility plus tests substitute for that reading. Whether that holds over years is exactly what the report says it doesn't yet know, the same gap between production and understanding that [Proof Generation vs Digestion](/posts/20260422-proof-generation-vs-digestion/) names for mathematics.

## Related Posts

- [Dive into Claude Code: The Design Space of Today's and Future AI Agent Systems](/posts/20260623-claude-code-design-space/): a source-level read of how a coding agent is actually architected, the design-space view this report's practices fit into.
- ["Skills" Are Not Software Engineering](/posts/20251124-skills-not-software-engineering/): the model-proposes, runtime-ensures-correctness split that the enforced invariants and linters here implement.
- [Filesystems vs. RAG](/posts/20260404-filesystem-vs-rag/): repository-as-context and "give Codex a map," the same reframing the report's knowledge base makes.
- [Proof Generation vs Digestion](/posts/20260422-proof-generation-vs-digestion/): production outrunning understanding, the open question behind a million unread lines.
