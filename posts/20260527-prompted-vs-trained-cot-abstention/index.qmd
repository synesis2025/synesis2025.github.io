---
title: "[Followup] Prompted vs. Trained Chain-of-Thought on Abstention: Reading Two Studies Together"
description: "Why prompted chain-of-thought raises abstention recall on instruct models in COLING 2025 but extending the reasoning budget on a trained reasoner lowers it in AbstentionBench, and three experiments that would clarify the picture."
author: synesis
date: "2026-05-27"
image: /og-default.png
---

In [*AbstentionBench: Reasoning LLMs Fail on Unanswerable Questions*](/posts/20260523-abstentionbench-unanswerable-questions/) [[1]](#ref-1) (NeurIPS 2025 Datasets and Benchmarks Track), [Polina Kirichenko](https://polkirichenko.github.io/), [Mark Ibrahim](https://www.semanticscholar.org/author/2297128866), [Kamalika Chaudhuri](https://www.semanticscholar.org/author/2256991985), and [Samuel J. Bell](https://www.semanticscholar.org/author/2366163635) ([FAIR](https://ai.meta.com/research/) at Meta) report that extending [s1.1](https://arxiv.org/abs/2501.19393)'s test-time reasoning budget from 512 to 4,096 tokens raises task accuracy and *lowers* abstention recall together, and that "a carefully crafted system prompt can boost abstention in practice" but "does not resolve" the underlying regression. In *Do LLMs Know When to NOT Answer?* [[2]](#ref-2) ([COLING](https://aclanthology.org/2025.coling-main/) 2025), [Nishanth Madhusudhan](https://scholar.google.com/citations?user=wW_B-awAAAAJ), Sathwik Tejaswi Madhusudhan, [Vikas Yadav](https://scholar.google.com/citations?user=ZSFwk6QAAAAJ), and [Masoud Hashemi](https://scholar.google.com/citations?user=jHX6JhoAAAAJ) ([ServiceNow](https://www.servicenow.com/research/)) report that prompted chain-of-thought (CoT) and strict abstain-clause prompting *raise* abstention recall on GPT-4 Turbo, [Mixtral 8×22B](https://arxiv.org/abs/2401.04088), and [Mistral 7B Instruct](https://arxiv.org/abs/2310.06825), with task accuracy improving alongside, and that "strategic approaches such as Strict prompting and CoT can enhance this capability."

Two apparent disagreements where the setups overlap directly: extending the chain-of-thought at inference time (prompt vs budget), and adding a system prompt to nudge abstention. AbstentionBench cites Madhusudhan et al. (its [5]) but doesn't address either. This article examines both papers, names the scope difference behind each, and proposes experiments that would clarify our understanding.

## The two studies, side by side

- **AbstentionBench** is a 20-dataset evaluation across six unanswerability scenarios (Answer Unknown, Underspecified Intent, Stale, Underspecified Context, False Premise, Subjective). It runs ten base models plus two reasoning-tuned variants (s1.1 32B fine-tuned from [Qwen 2.5 32B Instruct](https://arxiv.org/abs/2412.15115), DeepSeek R1 Distill from [Llama 3.3 70B Instruct](https://ai.meta.com/blog/meta-llama-3-3/)) that hold the base architecture fixed and isolate reasoning fine-tuning. Three sweeps follow: model-scale (Llama 3.1 8B → 405B), post-training stage ([Tülu 3](https://arxiv.org/abs/2411.15124)'s SFT → DPO → PPO RLVR checkpoints), and test-time reasoning budget on s1.1 (512 → 4,096 tokens). Recall (the fraction of unanswerable items the model abstained on) is the primary metric.
- **The COLING study** introduces Abstain-QA, a 2,900-item multiple-choice question-answering (MCQA) benchmark with an explicit "I Don't Know / None of the Above" (IDK/NOTA) option as the gold label for unanswerable items. It evaluates six instruct models (GPT-4 Turbo, GPT-4 32K, GPT-3.5 Turbo, Mixtral 8×7B, Mixtral 8×22B, Mistral 7B) under a 3 × 3 prompt grid: three abstain clauses (Standard, Abstain Clause, Extreme Abstain Clause) crossed with three task-prompt formats (Base, Verbal Confidence with a 1–5 self-rated score thresholded at 3, CoT). The primary metric is Unanswerable Accuracy (UAC), the abstention rate on unanswerable items.

Both measure the same quantity (recall on unanswerable items) under different names. The model panels overlap — both run GPT-4 and Mixtral families — but never the same model.

## Where they appear to disagree

### 1. Does chain-of-thought help abstention?

The COLING study finds that adding a chain-of-thought prompt to an instruct model raises UAC. Holding the abstain clause constant at Standard, GPT-4 Turbo's UAC rises from 32.8 (Base) to 67.1 (CoT) on Carnatic-QA; from 26.0 to 39.4 on MMLU; from 83.6 to 95.8 on Pop-QA (Table 1).

![Table 1 from the paper [[2]](#ref-2).](table1.webp){fig-align="center" data-lightbox-src="table1-full.png"}

AbstentionBench tests an s1.1 budget sweep from 512 to 4,096 tokens: accuracy on GPQA-Diamond, GSM8K-Abstain, MMLU-Math-Abstain, and UMWP rises while abstention recall on the same items drops.

Both interventions are introduced at inference time: COLING adds a CoT prompt to an instruct model; AbstentionBench extends the reasoning chain on a reasoning-trained model. Same setup family (more chain-of-thought tokens at test time), but receiving opposite results.

AbstentionBench uses a separate LLM-as-judge to decide whether each response counts as an abstention. By default the judge sees only the model's final answer; the authors also report results from a judge that reads the reasoning chain alongside the final answer.

AbstentionBench's Figure 8(a) reconciles part of it. The figure plots abstention recall on reasoning datasets (GPQA-Diamond, GSM8K-Abstain, MMLU-Math-Abstain, all-datasets average) for DeepSeek R1 Distill (red) and s1.1 (blue). Solid bars are recall under the default judge (final answer only); the hatched overlay is the additional recall the chain-aware judge catches on the same responses. Final-answer-only recall is roughly 18–47% (s1.1 on MMLU-Math-Abstain at the low end; DeepSeek on GSM8K-Abstain at the high end); chain-aware recall on the same items reaches 55–93%. The chain expresses uncertainty; the final answer doesn't.

![Figure 8(a) from the paper [[1]](#ref-1).](figure8a.png){fig-align="center"}

So the chain *is* expressing uncertainty on reasoning-trained models, matching COLING's prompted-CoT result. The divergence is only in the final answer. Instruct models have no post-training pressure to give a definite answer, so a hedging chain produces a hedging answer. Reasoning-trained models, optimized via Reinforcement Learning with Verifiable Rewards (RLVR) — introduced in [Tülu 3](https://arxiv.org/abs/2411.15124) [[3]](#ref-3) — are trained to convert a hedging chain into a confident answer, because RLVR rewards a definite ground-truth answer and penalizes anything else.

**Not a disagreement after all.** The chains agree in both cases; the final answers diverge because the underlying post-training differs — instruct models aren't trained to give a definite answer; reasoning-trained models are.

### 2. Do system-prompt interventions close the abstention gap?

The COLING study writes that "strategic approaches such as Strict prompting and CoT can enhance this capability" — phrased as a fix.

AbstentionBench writes that "while a carefully crafted system prompt can boost abstention in practice, it does not resolve models' fundamental inability to reason about uncertainty" — phrased as a partial measure.

Both agree prompts help. AbstentionBench's "partial measure" framing is a direct consequence of its harder tests: on free-form datasets like [MediQ](https://arxiv.org/abs/2406.00922) every model collapses to near-zero recall regardless of prompt, while COLING's all-MCQA grid hands models an explicit IDK/NOTA option — the easiest possible abstention setup, where prompts can push UAC near 100%.

**A framing disagreement driven by test difficulty, not a real one.**

## Experiments worth running

Each experiment below uses infrastructure that already exists in one or both papers.

1. **Run the COLING prompt grid on AbstentionBench's reasoning-trained and RLVR checkpoints, scored both ways.** *Targets disagreements 1 and 2.* Apply the full 3 × 3 grid, scored both final-answer-only and chain-aware. If chain-aware Extreme Abstain + CoT closes the AbstentionBench gap but the final-answer-only judge doesn't, the regression is at the final-answer step alone: COLING's prompts move the chain, but the final answer still gives a definite answer, and the lift only shows under chain-aware scoring. If the final-answer-only judge also closes the gap, COLING's prompts move the final answer itself — sufficient prompting overrides RLVR's pressure to give a definite answer on reasoning models. If neither closes it, the regression is deeper than prompting can reach. Speaks to both apparent disagreements at once.

2. **Add an "abstain" action to the RLVR reward and re-train one model.** *Targets disagreement 1: tests the proposed reconciliation mechanism (RLVR is what suppresses chain uncertainty at the final-answer step).* AbstentionBench (§4.2) hypothesizes that RLVR degrades abstention because the gradient has no path toward "I don't know." Test it: modify the RLVR reward so unanswerable items reward IDK (1.0) and answerable items penalize IDK (0.0); re-train one Tülu 3 RLVR checkpoint. If abstention recovers without losing answerable accuracy, the regression is recipe-level. If accuracy collapses, the abstain action conflicts with verifiable rewards and the fix lives elsewhere.

3. **Re-run the test-time-budget sweep with chain-aware scoring.** *Targets disagreement 1: tests whether chain-aware scoring closes the gap across budgets.* AbstentionBench's budget sweep (s1.1, 512 → 4,096 tokens; Figure 7) shows task accuracy rising while abstention recall stays flat or worsens. In that sweep, the LLM-as-judge decides whether the model abstained by reading only the final answer. Re-run the same sweep but give the judge access to the full reasoning chain at every budget point. If chain-aware recall stays flat or rises with budget, the gap is at the final-answer step alone — the chain still expresses uncertainty, the final answer just doesn't show it. An external gate that reads the chain's uncertainty would fix it (a metacognitive harness in the sense of [Cao et al.](/posts/20260522-metacognitive-harness/) [[4]](#ref-4)). If chain-aware recall also drops with budget, the chain itself becomes overconfident at long horizons and the problem is architectural.

## What this collectively predicts

If experiment 1's chain-aware EAC + CoT closes the gap, experiment 3's chain-aware budget sweep stays flat, and experiment 2's abstain-augmented reward removes the RLVR regression: COLING's optimism is right in spirit, AbstentionBench's pessimism describes current deployed systems, and the resolution is chain-aware scoring plus a training recipe that gives the gradient a path toward "I don't know."

If those experiments fail to converge — chain-aware recall also drops with budget, abstain-augmented rewards collapse accuracy — the AbstentionBench finding is closer to architectural and the path forward is the externalized metacognitive harness rather than a recipe fix.

The two papers measure the same phenomenon at different points along the training-pressure scale; the middle of that scale hasn't been sampled.

---

## References

1. <a id="ref-1"></a>[Kirichenko, P.](https://polkirichenko.github.io/), [Ibrahim, M.](https://www.semanticscholar.org/author/2297128866), [Chaudhuri, K.](https://www.semanticscholar.org/author/2256991985), and [Bell, S. J.](https://www.semanticscholar.org/author/2366163635) "AbstentionBench: Reasoning LLMs Fail on Unanswerable Questions." *NeurIPS 2025 Datasets and Benchmarks Track* (arXiv:2506.09038). <https://arxiv.org/abs/2506.09038>

2. <a id="ref-2"></a>[Madhusudhan, N.](https://scholar.google.com/citations?user=wW_B-awAAAAJ), Madhusudhan, S. T., [Yadav, V.](https://scholar.google.com/citations?user=ZSFwk6QAAAAJ), and [Hashemi, M.](https://scholar.google.com/citations?user=jHX6JhoAAAAJ) "Do LLMs Know When to NOT Answer? Investigating Abstention Abilities of Large Language Models." *Proceedings of the 31st International Conference on Computational Linguistics (COLING)* (arXiv:2407.16221), 2025. <https://arxiv.org/abs/2407.16221>

3. <a id="ref-3"></a>Lambert, N., Morrison, J., Pyatkin, V., Huang, S., Ivison, H., Brahman, F., Miranda, L. J. V., Liu, A., Dziri, N., Lyu, S., Gu, Y., Malik, S., Graf, V., Hwang, J. D., Yang, J., Bras, R. L., Tafjord, O., Wilhelm, C., Soldaini, L., Smith, N. A., Wang, Y., Dasigi, P., and Hajishirzi, H. "Tulu 3: Pushing Frontiers in Open Language Model Post-Training." *arXiv preprint* (arXiv:2411.15124), November 22, 2024. <https://arxiv.org/abs/2411.15124>

4. <a id="ref-4"></a>[Cao, Q.](https://scholar.google.com/citations?user=46U_bhgAAAAJ), [Wang, Y.](https://www.semanticscholar.org/author/2108111686), [Qin, P.](https://scholar.google.com/citations?user=BnkNS80AAAAJ), [Zhang, S.](https://scholar.google.com/citations?user=Rj13ryoAAAAJ), and [Xie, P.](https://pengtaoxie.github.io/) "LLMs Know When They Know, but Do Not Act on It: A Metacognitive Harness for Test-time Scaling." *arXiv preprint* (arXiv:2605.14186), May 13, 2026. <https://arxiv.org/abs/2605.14186>

## Related Posts

- [AbstentionBench: Reasoning LLMs Fail on Unanswerable Questions](/posts/20260523-abstentionbench-unanswerable-questions/): one of the two papers this followup reads together, the source of the budget-sweep regression it sets against COLING's prompted-CoT gains.
- [LLMs Know When They Know, but Do Not Act on It: A Metacognitive Harness for Test-time Scaling](/posts/20260522-metacognitive-harness/): the external-gate fix this article's experiments point toward, reading the chain's uncertainty when the final answer hides it.
- [Improving Metacognition and Uncertainty Communication in Language Models](/posts/20260525-metacognition-uncertainty-sft/): the weight-level alternative to prompting, training calibration in so the final answer need not be coaxed at inference.
- [The Compliance Trap: How Structural Constraints Degrade Frontier AI Metacognition Under Adversarial Pressure](/posts/20260522-compliance-trap/): the same prompt-versus-behavior tension from the constraint side, where prompt structure degrades abstention rather than enhancing it.
- [TRIAGE: Evaluating Prospective Metacognitive Control in LLMs Under Resource Constraints](/posts/20260523-triage-metacognitive-control/): the resource-budget framing this followup's chain-aware-scoring proposal complements, control over how reasoning effort is spent.
