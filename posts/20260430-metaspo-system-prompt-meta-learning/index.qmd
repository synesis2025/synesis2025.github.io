---
title: "System Prompt Optimization with Meta-Learning"
description: "MetaSPO meta-learns a task-agnostic system prompt that generalizes to 14 unseen tasks across 5 domains, lifting average performance over default + per-task user-prompt baselines."
author: synesis
date: "2026-04-30"
image: cover.jpg
---

![Generalization across different domains. Left: MetaSPO's performance on domains not used for prompt optimization, plotted against their similarity to the training domains. Right: Effect of the number of training domains, with more domains yielding stronger generalization. Figure 5 from the paper [[1]](#ref-1).](cover.jpg){fig-align="center"}

In *System Prompt Optimization with Meta-Learning* (NeurIPS 2025) [[1]](#ref-1), [Yumin Choi](https://www.mlai-kaist.com/), [Jinheon Baek](https://jinheonbaek.github.io/), and [Sung Ju Hwang](https://www.mlai-kaist.com/) (KAIST, DeepAuto.ai) introduce MetaSPO, a bilevel optimization framework that meta-learns a task-agnostic system prompt while simultaneously updating task-specific user prompts in an iterative inner loop.

> Large Language Models (LLMs) have shown remarkable capabilities, with optimizing their input prompts playing a pivotal role in maximizing their performance. However, while LLM prompts consist of both the task-agnostic system prompts and task-specific user prompts, existing work on prompt optimization has focused on user prompts specific to individual queries or tasks, and largely overlooked the system prompt that is, once optimized, applicable across different tasks and domains. Motivated by this, we introduce the novel problem of *bilevel system prompt optimization*, whose objective is to *design system prompts that are robust to diverse user prompts and transferable to unseen tasks*. To tackle this problem, we then propose *a meta-learning framework, which meta-learns the system prompt by optimizing it over various user prompts across multiple datasets, while simultaneously updating the user prompts in an iterative manner to ensure synergy between them*. We conduct experiments on *14 unseen datasets spanning 5 different domains*, on which we show that our approach produces system prompts that generalize effectively to diverse user prompts. Also, our findings reveal that the optimized system prompt enables *rapid adaptation even to unseen tasks, requiring fewer optimization steps for test-time user prompts while achieving improved performance*.

## Methods

![Concept figure. (A) An LLM input has three parts: task-agnostic system prompt, task-specific user prompt, target example. (B) Conventional task-specific optimization tunes the user prompt for one task and generalizes poorly. (C) MetaSPO meta-learns a system prompt that transfers to unseen target tasks. Figure 1 from the paper [[1]](#ref-1).](figure1.webp){fig-align="center" data-lightbox-src="figure1-full.png"}

Against this three-part structure, MetaSPO's bilevel algorithm runs two nested optimization loops. The outer loop optimizes the task-agnostic system prompt, and the inner loop iteratively refines task-specific user prompts. At each outer step, the current system prompt is paired with candidate user prompts across multiple source tasks. The model's failures on held-out examples feed an LLM optimizer that proposes refined candidates (user prompts inside the inner loop, system prompts at the outer loop). The meta-learned system prompt transfers to unseen target tasks with no further system-prompt tuning; at test time, only the user prompt is adapted per target task.

The system prompt starts as "You are a helpful assistant." (the Default baseline) and is iteratively refined through the outer loop. At each step of either loop, a separate optimizer LLM (GPT-4o mini in the main experiments, distinct from the Llama-3.2 3B base LLM that generates task responses) is prompted with the current prompt and its incorrectly-predicted examples, asked to analyze the failure modes, and then asked for multiple candidate refinements. Candidates are evaluated against held-out task data, and the top-k by performance survive to the next iteration. Both loops follow this same textual-gradient pattern (after Pryzant et al. [[4]](#ref-4)), applied at different scopes: user prompt per task in the inner loop, system prompt aggregated across source tasks in the outer loop.

![MetaSPO overview. The inner loop refines candidate user prompts via failure analysis on a target task; the outer loop refines the system prompt by aggregating failures across multiple source tasks. Figure 2 from the paper [[1]](#ref-1).](figure2.webp){fig-align="center" data-lightbox-src="figure2-full.png"}

## Results

The paper evaluates on 14 unseen target tasks across 5 domains (Medical, Review Analysis, Reasoning, Safety, Grounding), with each score the average performance of a system prompt paired with ten different user prompts. Within-domain Domain MetaSPO averages 44.5 over the 14 tasks, beating the next-best baseline SPRIG at 37.0 and the no-system-prompt Default at 32.2. The cross-domain Global MetaSPO setting (one system prompt meta-learned across all 5 domains) averages 41.4, still ahead of all baselines but ~3 points behind the within-domain Domain setting. Separately, on test-time efficiency: a MetaSPO-pretrained system prompt reaches Default's converged performance with 80% fewer optimization iterations and 75% less data during test-time user-prompt adaptation.

![Main results on 14 unseen target tasks, grouped by 5 domains. Global methods use a single system prompt across all domains; Domain methods use a system prompt meta-learned on the target's domain. Each cell is the average score across 10 different user prompts on that target task. Domain MetaSPO (bottom row) tops the overall average at 44.5. Table 1 from the paper [[1]](#ref-1).](table1.png){fig-align="center"}

Figure 4 tests whether MetaSPO's lift transfers uniformly across target tasks or depends on source-target task similarity. The paper measures similarity via Bag-of-Words overlap between the source-task training distribution and the target task, then plots per-task improvement against it.

![MetaSPO's gain over the Default baseline as a function of source-target task similarity (measured by Bag-of-Words overlap). Points above the y=0 line indicate a positive lift over Default; Pearson correlation 0.56 between similarity and improvement. High-similarity target tasks see larger gains; low-similarity tasks see near-neutral gains. Figure 4 from the paper [[1]](#ref-1).](figure4.png){fig-align="center"}

## My Notes

- **The decomposition is the contribution, more than the method.** Before this paper, "prompt optimization" treated the prompt as one optimization target for one task. DSPy [[2]](#ref-2), MIPRO [[3]](#ref-3), Pryzant et al.'s textual gradients [[4]](#ref-4), and [EvoPrompt](/posts/20260430-evoprompt/) [[5]](#ref-5) each target a different piece of that setup, but all produce task-specific prompts. The bilevel split (task-agnostic system prompt above, task-specific user prompt below) is the key move; once you make it, the meta-learning structure follows naturally because the outer loop has a natural "across tasks" loss to optimize. The MetaSPO algorithm itself is a structurally familiar inner/outer LLM-as-optimizer loop. What matters here is the decision about what to put in each level.
- **The system prompt does most of the transfer work.**
  - MetaSPO's headline wins on unseen tasks come from the system prompt. In Table 1's Unseen Generalization scenario, user prompts are LLM-sampled per task and held fixed; only the system prompt varies across the methods compared. Domain MetaSPO averages 44.5 over 14 unseen target tasks vs. Default's 32.2, a +12.3-point lift attributable to the system prompt alone.
  - Appendix B.7 tests system-prompt vs user-prompt optimization head-to-head: MetaSPO (system-prompt optimized on source tasks) vs. ProTeGi (user-prompt optimized on source tasks), evaluated on the unseen Reasoning domain. MetaSPO wins across every source-domain starting point; ProTeGi drops below the Default system prompt, indicating user-prompt optimizations don't transfer past the specific tasks they were tuned on.
- **Test-time efficiency is the practically relevant finding.** The headline number is the unseen-generalization performance, but the deployment-relevant result is in §4: a MetaSPO-pretrained system prompt reaches the Default baseline's converged performance with 80% fewer optimization iterations and 75% less data during test-time user-prompt adaptation. That's a recipe for shipping prompt-optimized agents when each new task gets a small calibration budget rather than full re-optimization.
- **Generalization is asymmetric along source-target similarity.** Figure 4 shows MetaSPO's gain over Default scales with source-target task similarity measured by Bag-of-Words. High similarity, big gain; low similarity, the meta-learned system prompt is closer to neutral. That's the expected meta-learning failure mode on out-of-distribution tasks, but it also bounds the deployment story: a system prompt meta-learned on Medical+Review+Reasoning will help most on tasks that look like those at the BoW level, and the cross-domain Global setting underperforms the within-domain Domain setting (41.4 vs. 44.5 avg). The follow-up question: at what point does the meta-learned prior become a *liability* on a sufficiently distant task?

---

## References

1. <a id="ref-1"></a>[Choi, Y.](https://www.mlai-kaist.com/), [Baek, J.](https://jinheonbaek.github.io/), [Hwang, S. J.](https://www.mlai-kaist.com/) "System Prompt Optimization with Meta-Learning." *Conference on Neural Information Processing Systems (NeurIPS)*, 2025. arXiv:2505.09666, May 14, 2025. <https://arxiv.org/abs/2505.09666>
2. <a id="ref-2"></a>Khattab, O., Singhvi, A., Maheshwari, P., Zhang, Z., Santhanam, K., Vardhamanan, S., Haq, S., Sharma, A., Joshi, T. T., Moazam, H., Miller, H., Zaharia, M., Potts, C. "DSPy: Compiling Declarative Language Model Calls into State-of-the-Art Pipelines." *International Conference on Learning Representations (ICLR)*, 2024. <https://arxiv.org/abs/2310.03714>
3. <a id="ref-3"></a>Opsahl-Ong, K., Ryan, M. J., Purtell, J., Broman, D., Potts, C., Zaharia, M., Khattab, O. "Optimizing Instructions and Demonstrations for Multi-Stage Language Model Programs." *Proceedings of the 2024 Conference on Empirical Methods in Natural Language Processing*, 2024. <https://arxiv.org/abs/2406.11695>
4. <a id="ref-4"></a>Pryzant, R., Iter, D., Li, J., Lee, Y., Zhu, C., Zeng, M. "Automatic Prompt Optimization with 'Gradient Descent' and Beam Search." *Proceedings of the 2023 Conference on Empirical Methods in Natural Language Processing*, 2023. <https://arxiv.org/abs/2305.03495>
5. <a id="ref-5"></a>Guo, Q., Wang, R., Guo, J., Li, B., Song, K., Tan, X., Liu, G., Bian, J., Yang, Y. "EvoPrompt: Connecting LLMs with Evolutionary Algorithms Yields Powerful Prompt Optimizers." *International Conference on Learning Representations (ICLR)*, 2024. arXiv:2309.08532. <https://arxiv.org/abs/2309.08532>

## Related Posts

- [DSPy: Compiling Declarative Language Model Calls into Self-Improving Pipelines](/posts/20260430-dspy/): the prior-art prompt-optimization substrate MetaSPO cites, which it extends by splitting the prompt into a transferable system level and a task-specific user level.
- [EvoPrompt: Connecting LLMs with Evolutionary Algorithms Yields Powerful Prompt Optimizers](/posts/20260430-evoprompt/): an earlier LLM-as-optimizer loop that, like every method before MetaSPO, produces task-specific prompts rather than a task-agnostic one.
- [Multi-Agent Design: Optimizing Agents with Better Prompts and Topologies](/posts/20260430-multi-agent-system-search/): the same staging-is-the-contribution pattern, adding a search wrapper around an off-the-shelf prompt optimizer instead of a bilevel meta-learning loop.
- [GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning](/posts/20260513-gepa/): a textual-gradient prompt optimizer in the same lineage as the Pryzant-style inner loop MetaSPO builds on.
- [Truly Self-Improving Agents Require Intrinsic Metacognitive Learning](/posts/20260430-intrinsic-metacognitive-learning/): the position paper that classifies MetaSPO's hand-fixed search structure as extrinsic, with humans choosing what each level optimizes.
